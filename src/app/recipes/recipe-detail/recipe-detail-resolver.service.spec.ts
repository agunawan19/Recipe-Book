import { RecipeDetailResolverService } from './recipe-detail-resolver.service';

describe('RecipeDetailResolverService', () => {
  it('should create an instance', () => {
    expect(new RecipeDetailResolverService()).toBeTruthy();
  });
});
