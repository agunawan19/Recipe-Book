import { RecipeDetailResolver } from './recipe-detail-resolver';

describe('RecipeDetailResolver', () => {
  it('should create an instance', () => {
    expect(new RecipeDetailResolver()).toBeTruthy();
  });
});
